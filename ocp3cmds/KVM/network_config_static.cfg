network-interfaces: |
  auto eth0
  iface eth0 inet static
  address ${prefixIP}.${octetIP}
  network ${prefixIP}.0
  netmask 255.255.255.0
  broadcast ${prefixIP}.255
  gateway ${prefixIP}.1
  dns-nameservers ${prefixIP}.1
